import React from 'react'

export const metadata = {
    title: "PRODUCTS DETAILS PAGE",
    description: "PRODUCT"
}

const ProductDetailsPageLyout = ({children}) => {
  return (
    <>{children}</>
  )
}

export default ProductDetailsPageLyout