import React from 'react'

export const metadata = {
    title: "PRODUCTS PAGE",
    description: "PRODUCTS"
}

const ProductsPageLyout = ({children}) => {
  return (
    <>{children}</>
  )
}

export default ProductsPageLyout